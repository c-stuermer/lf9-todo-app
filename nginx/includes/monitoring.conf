# monitoring.conf - optional routes for monitoring tools
# Uncomment and adapt if you want to route Grafana through the reverse proxy.
# Requires Grafana to be configured with GF_SERVER_ROOT_URL and GF_SERVER_SERVE_FROM_SUB_PATH.

# location /grafana/ {
#     proxy_pass http://grafana:3000;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
# }
